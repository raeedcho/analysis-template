# Trialframe composition config
# Maps signal names to file suffixes for loading related parquet files.
info: "meta"
composition:
  state: "states"
  hand position: "hand-pos"
  # motor cortex: "neural-lfads-rates"
